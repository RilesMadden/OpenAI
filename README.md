Code used during OpenAI Tech Support take-home assessment.

Code assumes there is a file in the local directory named `config_file.py` containing value `api_key` which corresponds to a valid OpenAI API key.
